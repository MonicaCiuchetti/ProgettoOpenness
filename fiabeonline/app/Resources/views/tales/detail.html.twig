{% extends 'base.html.twig' %}
{% block body %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-2">
            </div>
            <div align="center" class="col-sm-4">
                {% for tale in tales %}
                    <h3>Titolo: {{ tale.0.taleTitle }}</h3>
                {% endfor %}
            </div>
            <div class="col-sm-2"></div>
            <div class="col-sm-2"><h3>CARTE</h3></div>
            <div class="col-sm-2">
                <div class="dropdown">
                    <button class="btn btn-default dropdown-toggle" type="button" id="dropdownMenu1" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
                        filtro
                        <span class="caret"></span>
                    </button>
                    <ul class="dropdown-menu" aria-labelledby="dropdownMenu1">
                        <li><a href="#">Visualizza solo testo</a></li>
                    </ul>
                </div>
            </div>
        </div>
        {# todo for cicle#}
        <div class="row">
            <div class="col-sm-2">Inizio{# #}</div>
            <div class="col-sm-4">
                <div class="jumbotron">
                    inizio fiaba
                </div>
            </div>
            <div class="col-sm-2">{# immagini carte #}</div>
        </div>
    </div>

{% endblock %}